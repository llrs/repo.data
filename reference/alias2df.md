# Create a matrix of alias

Joins the matrices of each file

## Usage

``` r
alias2df(x)
```

## Arguments

- x:

  A raw alias output as given by CRAN (and Bioconductor).

## Value

A matrix with Package, Source (The name of the file), and the Target (or
Alias)
